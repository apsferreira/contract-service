# =============================================================================
# Contract Service Frontend — Traefik IngressRoute
# Domain: contract.institutoitinerante.com.br → contract-service-frontend:80
# (sem 's' — alinhado com CF Tunnel)
# =============================================================================
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: contract-service-frontend
  namespace: production
  labels:
    app: contract-service-frontend
    component: frontend
    part-of: instituto-itinerante
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`contract.institutoitinerante.com.br`)
      kind: Rule
      services:
        - name: contract-service-frontend
          port: 80
